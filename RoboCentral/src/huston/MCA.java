package huston;

import java.util.ArrayList;

import com.jcraft.jsch.jce.Random;

public class MCA {
	
	
	private ArrayList<Integer> partMenge = new ArrayList<>();
	private final static int M = 1000; 
	private int r1,r2;
	
	public ArrayList<Integer> generateParticles(int p){
		partMenge.clear();
		int i = 0;
		
		while( i < M ) {
			r1 = (int) (Math.random()*6000);
		}
		
		
		return partMenge;
	}

}
